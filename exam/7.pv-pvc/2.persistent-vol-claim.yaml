apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: testbed
  name: pvc1 
spec:
  resources:
    requests:
      storage: 10Gi 
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: standar
  selector:
    matchLabels:
      app: pv1